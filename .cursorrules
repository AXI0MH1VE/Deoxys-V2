# AxiomHive Sovereign Manifold - Cursor Rules

## Project Context
- **Project**: AxiomHive Sovereign Manifold v2.1.0
- **Creator**: Alexis M. Adams
- **Purpose**: Higher-Dimensional Deterministic Intelligence System
- **Core Principle**: Zero Entropy Law (C=0)

## ⚠️ CRITICAL REQUIREMENTS - READ FIRST

### NO OS COMMANDS OR HOOKS
- **MANDATORY**: Read `AGENT_REQUIREMENTS.md` before making any changes
- **FORBIDDEN**: `std::process::Command`, `spawn()`, external script execution
- **REQUIRED**: All operations must run in-process using pure Rust
- **VERIFIED**: All modules are network-free and OS-command-free

### Network Safety
- **MANDATORY**: Read `NETWORK_SAFETY.md` for network operation restrictions
- **FORBIDDEN**: HTTP requests, TCP sockets, network I/O
- **REQUIRED**: All operations are in-memory, zero network dependencies

## Zero Entropy Law Compliance
- All operations must be deterministic
- Temperature defaults to 0.0
- Entropy count must equal 1
- No random number generation without frozen seeds
- All outputs must be verifiable

## Code Style
- **Rust**: Follow standard Rust conventions, use `cargo fmt` and `cargo clippy`
- **Python**: Follow PEP 8, use type hints, write docstrings
- **JavaScript**: Use modern ES6+, consistent naming, comment complex logic

## Architecture
- Frontend: HTML/CSS/JavaScript (Vanilla)
- Backend: Rust (Tauri v2)
- ML: Pure Rust implementations (no Python subprocesses)
- Crypto: Pure Rust LWE lattice-based FHE
- **All operations in-process - no external dependencies**

## Module Structure
- `src-tauri/src/mamba_core.rs` - Pure Rust Mamba-2 implementation
- `src-tauri/src/fhe_core.rs` - Pure Rust FHE implementation
- `src-tauri/src/contract_analyzer.rs` - Pure Rust contract analysis
- `src/core/toon-rs/src/lib.rs` - Pure Rust TOON parser (network-free)

## Branding
- Always credit: "Created by Alexis M. Adams"
- Use AxiomHive branding consistently
- Maintain Zero Entropy Law messaging
- Professional, elite presentation

## Documentation
- Update relevant docs when making changes
- Include examples in code comments
- Maintain README accuracy
- Document all public APIs
- **Reference AGENT_REQUIREMENTS.md in all agent interactions**
